import React from "react";
import { Layout } from "../../components/Layout/Layout";

export const About = () => {
    return (
        <Layout>
            <div>Work in Progress...</div>
        </Layout>
    )
}